import java.util.*;

public class Q4 {
    public static void main(String[] args) {
        TreeSet<Integer> set = new TreeSet<>(Arrays.asList(5, 15, 25, 35, 45));

        int limit = 25;

        set.removeIf(x -> x < limit);

        System.out.println("After removing elements < " + limit + ": " + set);
    }
}
